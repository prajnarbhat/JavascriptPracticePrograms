// array.shift removes the first element from the array
// array.shift also return that 1st element removed

const array = [1,2,3,4];

const arrayShift = array.shift();

console.log("Give the first removed element:",arrayShift);