// array.join is used to join the elements of array to string

const array = ['a','b','c'];

const arrayToString = array.join('');

console.log("array to string:",arrayToString);

// Array.join created one array from all the array elements
// It joins all the array elements and create a single string from that
