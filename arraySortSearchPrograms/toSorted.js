// sort the elements of array alphabeically
// It will not change the original array

const fruits = ["Banana", "Orange", "Apple", "Mango"];

const sortedfruits = fruits.toSorted();

console.log("Sorted elements:",sortedfruits);
console.log("Original array will not be changed:",fruits);