// sort the elements alphabeically
// It will change the original array as well

// .sort works only with strings
// .sort compare the string elements that it compares B with A

const fruits = ["Banana", "Orange", "Apple", "Mango"];
const sortedElements = fruits.sort();

console.log("Sorted elements alphabeticaly:",sortedElements);
console.log("Original array:",fruits);