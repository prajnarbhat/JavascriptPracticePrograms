let object1 = { key1 : 'value1', key2 : 'value2', key3 : 'value3'}

// To check the type of the variable
console.log("Check type of value:",typeof(object1))

// To check if any instance is made with a certain constructot
// it return a boolean value
console.log("check type:",object1 instanceof Object)
console.log("check type:",object1 instanceof float)